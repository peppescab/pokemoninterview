package it.to.peppesca.domain.models

/**
 * Entity model for domain layer.
 *
 * @property id as id of pokemon.
 * @property name as the name of this pokemon.
 */
data class PokemonEntity(val id: Int, val name: String)